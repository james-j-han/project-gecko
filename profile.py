

class Profile:

	def __init__(self, profile_id, profile_name, first_name, last_name, email, phone, same_as_shipping, shipping_address, shipping_address_2, shipping_city, shipping_state, shipping_zip, billing_address, billing_address_2, billing_city, billing_state, billing_zip):
		self.profile_id = profile_id
		self.profile_name = profile_name
		self.first_name = first_name
		self.last_name = last_name
		self.email = email
		self.phone = phone
		self.same_as_shipping = same_as_shipping
		self.shipping_address = shipping_address
		self.shipping_address_2 = shipping_address_2
		self.shipping_city = shipping_city
		self.shipping_state = shipping_state
		self.shipping_zip = shipping_zip
		self.billing_address = billing_address
		self.billing_address_2 = billing_address_2
		self.billing_city = billing_city
		self.billing_state = billing_state
		self.billing_zip = billing_zip